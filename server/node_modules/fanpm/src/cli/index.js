var shell = require('shelljs')
var chalk = require('chalk')
var path = require('path')
function run(params) {
	
	shell.echo(chalk.red(shell.exec( `node ${path.resolve(__dirname, './a.js')}`, { silent: true }).stdout))
	// shell.echo(chalk.red(shell.which('git')))
	// shell.echo(chalk.red(shell.which('git')))
	// console.log(__dirname);
	// shell.exit(1)
	console.log('run ...1');
}
module.exports = {
	run
}